---
const { eyebrow = "", title = "", subtitle = "" } = Astro.props;
---
<div class="max-w-2xl">
  {eyebrow && (
    <div class="mb-3 text-xs font-semibold uppercase tracking-[0.22em] text-cyanBrand-500/90">
      {eyebrow}
    </div>
  )}
  <h2 class="font-display text-3xl font-semibold leading-tight sm:text-4xl">{title}</h2>
  {subtitle && <p class="mt-3 text-white/75">{subtitle}</p>}
</div>
